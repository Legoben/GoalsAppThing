/**
 * App config
 */
window.config = {
    api: "http://ping.ngrok.com/"
};

/**
 * Initialiser
 */
$(document).ready(function() {
    // bind a bunch of stuffs pls.
    $(".home .btn-join").click(function(e) {
        e.preventDefault();

        alert("plsss m8");
    });
});